export default {
  userId(state) {
    return state.userId
  },

  appInfo(state) {
    return state.appInfo
  },

  isApp(state) {
    return state.isApp
  },

  isShowMask(state) {
    return state.isShowMask
  }
}